# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）。
# docker-compose.yml 通过 ${VAR} 从这里读取。

# Telegram Bot（@BotFather 获取）
TELEGRAM_BOT_TOKEN=
# 接收推送 / 唯一被授权发命令的 chat_id
TELEGRAM_CHAT_ID=
# 可选：额外授权发命令的 chat_id，逗号分隔
TELEGRAM_ALLOWED_CHAT_IDS=
